#define CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<math.h>
int main()
{
	int x, y;
	for (x = 1; x < 50; x++)
	{
		for (y = 1; y < 50; y++)
		{
			if (x * x + y * y == 1989)
			{
				printf("%d*%d+%d*%d=%d\n", x, x, y, y, 1989);
			}
		}
	}
}
