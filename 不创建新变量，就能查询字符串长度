#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int strlen(char* p)
{
	if (*p != '\0')
		return 1 + strlen(p + 1);
	else
		return 0;
}
int main()
{
	char arr[] = "bit";
	int len = strlen(arr);
	printf("len = %d\n", len);
	return 0;
}
